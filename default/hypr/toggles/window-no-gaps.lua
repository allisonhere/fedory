-- Based on basecamp/omarchy default/hypr/toggles/window-no-gaps.lua with Fedory command and path names.
-- Remove all window gaps and borders.
hl.config({
  general = {
    gaps_out = 0,
    gaps_in = 0,
    border_size = 0,
  },

  decoration = {
    rounding = 0,
  },
})
