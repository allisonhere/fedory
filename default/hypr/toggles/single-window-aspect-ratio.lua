-- Based on basecamp/omarchy default/hypr/toggles/single-window-aspect-ratio.lua with Fedory command and path names.
-- Avoid overly wide single-window layouts on wide screens.
hl.config({
  layout = {
    single_window_aspect_ratio = { 1, 1 },
  },
})
