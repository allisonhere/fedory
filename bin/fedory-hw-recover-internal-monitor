#!/bin/bash

# fedory:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/fedory/toggles/hypr/internal-monitor-disable.lua"

if [[ -f $TOGGLE ]] && ! fedory-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
