#!/bin/bash

# fedory:summary=Refresh the SDDM theme from default
# fedory:requires-sudo=true

sudo rm -rf /usr/share/sddm/themes/fedory
sudo cp -r "$FEDORY_PATH/default/sddm/fedory" /usr/share/sddm/themes/fedory
