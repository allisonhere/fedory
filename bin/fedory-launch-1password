#!/bin/bash

# fedory:summary=Launch 1Password or start its installer when missing.

set -e

if fedory-cmd-present 1password; then
  exec setsid uwsm app -- 1password
else
  exec fedory-launch-floating-terminal-with-presentation fedory-install-service-1password
fi
