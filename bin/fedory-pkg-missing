#!/bin/bash

# fedory:summary=Returns true if any of the named packages are missing from the system (or false if they're all there).
# fedory:args=<packages...>

for pkg in "$@"; do
  if ! rpm -q "$pkg" &>/dev/null; then
    exit 0
  fi
done

exit 1
