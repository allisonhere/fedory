#!/bin/bash

# fedory:summary=List available monospace fonts
# fedory:examples=fedory font list | fedory font set "CaskaydiaMono Nerd Font"

fc-list :spacing=100 -f "%{family[0]}\n" | grep -v -i -E 'emoji|signwriting|fedory' | sort -u
