#!/bin/bash

# fedory:summary=Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.local/state/fedory/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  FEDORY_THEME_SKIP_BACKGROUND=1 fedory-theme-set "$(cat $THEME_NAME_PATH)"
fi
