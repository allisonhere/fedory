#!/bin/bash

# fedory:summary=Apply the current theme keyboard color to ASUS ROG keyboards
# fedory:hidden=true

ASUSCTL_THEME=$HOME/.local/state/fedory/current/theme/keyboard.rgb

if fedory-cmd-present asusctl; then
  asusctl aura effect static -c $(sed 's/^#//' $ASUSCTL_THEME)
fi
