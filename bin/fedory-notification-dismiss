#!/bin/bash

# fedory:summary=Dismiss a notification by summary substring. Used by the first-run notifications to dismiss them after clicking for action.
# fedory:args=<summary>

if (($# == 0)); then
  echo "Usage: fedory-notification-dismiss <summary>"
  exit 1
fi

fedory-shell -q notifications dismiss "$1"
