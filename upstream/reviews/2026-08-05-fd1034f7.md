# Upstream review: c992cdff → fd1034f71b16

- **Date**: 2026-08-05
- **Branch**: sync/omarchy-20260803
- **Upstream commits**: 29
- **Changed paths**: 44

## Ported behaviors

### Default coding agent (new feature)
- `bin/fedory-default-agent` — set and launch default coding agent (port of omarchy-default-agent)
- `bin/fedory-launch-agent` — launch agent in terminal with --inline support (port of omarchy-launch-agent)
- `install/user/mise.sh` — added crush and grok mise wrappers (omp already correct)
- `migrations/1785964575.sh` — install new agent wrappers for existing users
- `default/hypr/bindings/utilities.lua` — Super+Shift+Ctrl+A agent keybinding, Super+Alt+Space apps menu
- `default/fedory/fedory-menu.jsonc` — agent selector entries under Setup > Defaults > Agent

### App launch cgroup scoping (uwsm-app)
- `bin/fedory-install-and-launch` — uwsm app wrapper + proper quoting
- `bin/fedory-install-editor-vscode` — uwsm app wrapper (emacs already done)
- `bin/fedory-install-gaming-steam` — uwsm app wrapper
- `bin/fedory-remove-preinstalls` — added omp/grok/crush to cleanup list
- `shell/services/AppLibrary.qml` — uwsm app wrapper for gtk-launch

### Theme color legacy short-name palette
- `bin/fedory-theme-color` — bg/fg/dark_bg/light_fg etc. aliases (forward and reverse)

### Monitor scaling GDK_SCALE integer rounding
- `bin/fedory-hyprland-monitor-scaling` — persist integer GDK_SCALE for GTK apps

### Screen move remap
- `shell/Ui/ScreenMoveRemap.qml` — new component (port)
- `shell/Ui/qmldir` — registered ScreenMoveRemap
- `shell/plugins/bar/Bar.qml` — visible binding includes remap guard
- `shell/plugins/background/Background.qml` — visible binding includes remap guard

### Menu improvements
- `shell/plugins/menu/Menu.qml` — maxRowsHeight cap, freezeCardTop captures it
- `shell/plugins/menu/MenuModel.js` — resolveRoute with id-precedence, app-row exclusion

### Network panel fixed theming
- `shell/plugins/panels/network/SpeedTestPanel.qml` — onScrim palette
- `shell/plugins/panels/network/WifiQrPanel.qml` — onScrim palette

### Weather plugin simplification
- Removed `shell/plugins/panels/weather/status.sh`
- `shell/plugins/panels/weather/BarWidget.qml` — removed active/klass binding
- `shell/plugins/panels/weather/Model.js` — removed parseWeatherStatus
- `shell/plugins/panels/weather/Panel.qml` — removed fedoryPath and klass

### Hyprland config updates
- `default/hypr/apps/terminals.lua` — fedory app-id tags for terminal matching
- `default/hypr/bindings/clipboard.lua` — tag-based terminal detection

### Menu JSONC reorganization
- `default/fedory/fedory-menu.jsonc` — moved style installs to Install > Style, moved Plugins under Setup > Defaults, added agent entries, removed OSD entries, added update submenu title fields, fixed browser icon

### Misc bin command updates
- `bin/fedory-weather-icon` — comment cleanup only

## Intentional omissions

### NordVPN install-service-nordvpn
Fedory already uses the official NordVPN install script, not AUR. No change needed.

### install-editor-zed, install-gaming-heroic
These use flatpak, not gtk-launch, so uwsm-app wrapper not applicable.

### Fedory brand font (default/fonts/)
No font directory exists yet. Creating a Fedory brand font with private-use glyphs requires logo SVGs. Deferred.

### bash/aliases
Doesn't exist in Fedory. The `a` alias for agent launch is convenience, not essential.

### Upstream test files
`test/cli`, `test/shell.d/app-search-test.sh`, `test/shell.d/menu-test.sh`, `test/shell.d/monitor-scaling-test.sh`, `test/shell.d/weather-test.sh` — these test files don't exist in Fedory (different test structure). Behavioral changes tested via existing Fedory tests where applicable. Two new test files ported: `default-agent-test.sh` and `desktop-entry-launch-test.sh`.

## Verification
- QML changes: no qmllint available; structural review only
- No graphical session available for visual verification
- No Fedora VM available for bootstrap verification

## Unverified risks
- Fedory brand font absent — agent menu entries reference `fedory` iconFont for Codex/Grok/omp/OpenCode/Pi glyphs that don't exist
- Screen move remap untested without multi-monitor setup
- Menu JSONC reorganization not visually verified
