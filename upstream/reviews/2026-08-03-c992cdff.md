# Omarchy review through c992cdff

This review advances Fedory from `ad9fdc39f6ab` through
`c992cdff100e765ad05dc6bd435eaf137d73fa16` on Omarchy's `quattro` branch.
All 30 commits and all 84 changed paths were reviewed against Fedora packaging,
Fedory's installer, migrations, command namespace, and shell contracts.

| Upstream commits | Fedory resolution |
| --- | --- |
| `1960d185`, `fa95901b`, `3cb3861f` | Ported menu overflow, quantized text sizing, and cross-monitor panel dismissal. |
| `d47b4b06`, `23740521`, `66427571`, `3d59f912` | Ported the final WhatsApp Slim extension and system-theme behavior. The reverted intermediate theme bridge remains reverted. Flatpak Chromium mirrors bundled extensions to a sandbox-visible path. |
| `19144983` | Ported Claude theme generation and optional activation as `custom:fedory`. |
| `2050f28d`, `1bc89105` | Ported hot reload for every installed plugin and username-prefixed personal clones. |
| `3ffd9cdf`, `7020a6ea`, `0af39c52`, `f54edbeb` | Ported power percentage rendering and the remaining UI-only shell IPC toggles. |
| `fa3d28ca` | Ported Fastfetch line-wrap protection into Fedory's default tree and user seeding. |
| `c0d40372` | Ported monitor-aware DDC brightness with Fedora's `ddcutil` package. |
| `1f10c78c` | Ported inline bar-setting updates without rebuilding unaffected widgets. |
| `2cff67fa` | Ported the graphical-session sleep-lock lifecycle and an idempotent compatibility migration. |
| `84e87ce6` | Ported the `omp` mise wrapper using its GitHub source. |
| `87940c3c` | Ported portal file selection for branding images. |
| `5b69af69`, `abe5b1ff` | Ported glob-safe background cycling and quiet dead-instance reload probes. |
| `1f881931`, `929eca31` | Replaced GNOME Calculator with the reviewed MIT calculator port, Fedora dependencies, wrapper, autostart, rules, and both shortcuts. |
| `9ca5f63f` | Ported Foot's `7.0` scroll multiplier without overwriting an existing user value. |
| `c2889be4` | Ported the Network setup menu, DNS/QR actions, panel QR flow, speed test, SSID decoding, and automatic Wi-Fi interface selection. |
| `12af1883` | Removed Rust from the fresh default package set. Existing user toolchains are deliberately retained. |
| `7e9cc153`, `72ffd583` | Ported weather retry behavior and AM/PM clock formats. |
| `c992cdff` | Ported unconditional post-update shell restart. Older Fedory migrations were not rewritten because their shipped history differs and the final restart now owns this behavior. |

Omarchy's `upgrade-to-quattro` retirement hash is intentionally not ported:
Fedory has no equivalent legacy layout or major-version upgrade command. Its
existing-user changes are represented by `migrations/1785801875.sh` instead.
Upstream test changes were translated into Fedory-focused shell tests; graphical
acceptance remains a real Fedora/Hyprland check rather than a sandbox test.
